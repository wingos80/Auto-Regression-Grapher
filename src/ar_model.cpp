// ar_model.cpp is intentionally empty.
// ARModel is a class template — its full definition lives in ar_model.h.
// Template bodies must be visible at every point of instantiation,
// so they cannot be split into a .cpp file the normal way.
#include "ar_model.h"